# 开发注意事项与命名规则（本项目实战沉淀）

> 来源：DSH 桌面化项目（Electron 主路线）全程实测。写新代码、改构建、换环境前先过一遍。
> 配套：《Electron施工计划与进度.md》的恢复协议（检查点协议）+ 各阶段实测记录。

---

## 一、环境与沙箱约束（本机 AI 会话内执行命令时）

1. **子进程管道受限**：Node 内 `spawn`/`spawnSync` 用默认 pipe stdio 会 `EPERM`；用 `stdio:'inherit'`/`'ignore'` 或文件描述符重定向（`openSync` + `stdio:[fd,fd]`）。PowerShell 自己的管道不受影响。
2. **后台会话无 UI**：SmartScreen / Smart App Control / 证书导入等需要交互确认的操作会**无声挂起**（进程活着、无输出、无窗口）。安装器计时、证书信任必须由用户在交互会话完成。
3. **执行环境是 PowerShell 5.1 语义**（非 pwsh 7）：`Get-Content -AsByteStream` 等 7.x 语法不可用；读字节用 `[System.IO.File]::ReadAllBytes`（需完整权限上下文）。
4. **npm 垫片不可 spawn**：Windows 上 `spawn('npm')` 报 ENOENT（本机 PATH 甚至只有 npm.ps1）。npm 随 Node 发布：从 `dirname(process.execPath)/node_modules/npm/bin/npm-cli.js` 推导，用 node 直调。
5. **registry 直连可能无限挂起**：无代理环境访问 registry.npmjs.org 会挂（无报错）；构建脚本强制 `npm_config_registry=https://registry.npmmirror.com`；Electron 二进制同理用 `ELECTRON_MIRROR` npmmirror。
6. **junction 创建半途失败会留"真目录"毒化**：受限模式下 `healProfilesModuleFallback` 的 junction 建一半会留真目录，后续报"exists and is not a symlink"。测试 home 用完即删或整目录重建。
7. **setx 只对新进程生效**：改环境变量后已存在的进程（含 AI 会话）读不到；脚本内显式带 `$env:...`。

## 二、Windows / Node 通用坑

1. **spawn 参数用数组**：路径含空格（`Program Files`）时用字符串拼接会被拆；数组参数 + 文件重定向是唯一稳的形态。
2. **GUI 进程不等返回**：PowerShell `&` 不等待 GUI 程序；用 `Start-Process -PassThru -Wait` 或 `cmd /c`（后者对 GUI 进程也会等待）。
3. **CJS 命名导入**：Node 24 的 ESM-CJS 互操作检测不到某些 CJS 包的命名导出（如 electron-updater），运行时报 `SyntaxError: Named export not found`——一律 `import pkg from 'cjs-pkg'; const { x } = pkg`。
4. **`$home` 在 PowerShell 是只读自动变量**：脚本变量名避开 `$home/$pid/$pwd` 等。
5. **pwsh 读 UTF-8 无 BOM 文件可能按 GBK 解码**（显示乱码/JSON 解析失败）：脚本里读 JSON 用 `node` 或 `-Encoding UTF8`。

## 三、Electron / electron-builder 坑（源码级实证）

1. **extraResources 的 from 不能以 node_modules 为根**：app-builder-lib 的拷贝 filter 硬编码排除拷贝根一级的 `node_modules`（静默跳过，只剩 package.json）。解法：`from` 上提一层父目录。
2. **asarUnpack 是硬要求**：`*.node` 与 `@img/**` 平台二进制在 asar 内不可被系统加载器读取。
3. **RUN_AS_NODE 宿主子进程不依赖 asar 支持**：一切给子进程读的文件（vendor/profile、desktop.patch.yml、icon）走 `extraResources` 放 resources/ 下，路径用 `app.isPackaged ? process.resourcesPath : 开发目录` 分支。
4. **打包态未捕获异常 = 原生对话框挂起**（进程不死、无日志、无窗口）：main 的第一个 import 必须是 `early-errors.mjs`（uncaughtException/unhandledRejection 落盘）。
5. **Electron 内建 Node 与系统 Node 行为差异**：cordis-loader 在 Electron Node 下拿不到 internal 句柄，`dsh web` 的 hmr 回退要求 `--expose-internals` V8 旗标（系统 Node 无需）。M0 断言必须以目标运行时实测，不信纸面版本。
6. **安装速度 ∝ 文件数**：NSIS 解压 + Defender 逐文件扫描是大头；剪枝优先级 = 文件数 > 字节数（`.map`/`.d.ts`/`.md`/LICENSE/test/docs 目录全剪，约减半文件数）。
7. electron-builder 自带 NSIS 工具链，构建机**无需装 makensis**；`node node_modules\electron-builder\out\cli\cli.js` 直调避免 npx 垫片。
8. **`ELECTRON_RUN_AS_NODE=1` 环境泄漏**（0.4.2 实证）：桌面壳宿主子进程会带该变量；若泄漏到"启动壳本身"的环境（AI 会话继承、宿主内二次拉起），主进程退化成纯 Node，`import { app } from 'electron'` 报"无此导出"、无窗口无日志。主进程有 `src/node-guard.mjs` 兜底报错；AI 会话里跑任何 electron 前先 `Remove-Item Env:ELECTRON_RUN_AS_NODE`。
9. **Chromium 禁 http 页面加载 `file://` 本地资源**（0.4.2 实证，背景图失效根因）：渲染器报 "Not allowed to load local resource"。给 SPA 用的本地文件一律走壳回环 HTTP 供给（admin `/bg-image`：MIME 白名单 + `Cache-Control: no-store` + `?t=mtime` 破缓存）。
10. **签名时间戳服务器不可达**（0.4.1 实证）：signtool 时间戳（digicert/microsoft 系）本机网络 ETIMEDOUT → 带 CSC 环境变量构建必失败；无网络出**未签名包**（SmartScreen 首次提示属预期）。
11. **dist 新旧版本共存易误发**：`dist/` 多版本 exe 并存，发人前核对文件名；发新版后清旧包，README 产物行同步。
12. **netstat pid→端口**：不依赖 WMI/CIM（可能被禁且慢）；`netstat -ano -p tcp` 每行末列为 PID。
13. **rc.6 native 目录选择器崩溃**（0.4.4 实证）：koffi COM worker 选取时静默消失（无 stderr）→ "win32 folder dialog worker exited before reporting a result"；宿主 env 注入 `SSH_CONNECTION=dsh-desktop-browse` 钉住应用内浏览（vendor 全树仅此一处读该变量）。
14. **asar 热更新**（0.4.4 实证）：必须 `@electron/asar` extract 已装 asar 再覆写打包（asar 含 electron-updater 等生产依赖，从零打包会丢依赖）；`dsh-desktop-ui/lib/client.js` 不在 asar，另有已装 vendor + HOME profile 两份副本，热更新要都换。
15. **打包产物 --smoke 退出码不可信**（AI 沙箱下）：成功也可能 `-2147483645`(0x80000003)，**以日志 SMOKE OK 为准**。
16. **打包 / asar 热更新必须经用户同意**（2026-09-08 用户确认的硬规矩）。
17. **`scripts/` 不进包，门禁代码必须落 `src/`**（S2 实证）：`electron-builder.yml` 的 `files` 只有
   `src/**` + `VERSION` + `package.json`。`abi-scan.mjs` 原在 `scripts/`，**打包后根本不存在**，
   而装好的应用要能重建 vendor 树、且门禁是唯一防线（无回滚备份）。已搬进 `src/vendor-build.mjs`，
   `build-host`/`abi-scan` 退化成 CLI 薄封装。**新增会被打包态调用的能力一律不能放 `scripts/`。**
18. **`vendor.lock.json` 的体积基线是 pnpm 建的，别拿它当同构证据**（S2 实证）：现网 `vendor/profile`
   含 `node_modules/.pnpm`/`pnpm-lock.yaml`/`.modules.yaml` 且无 npm 锁文件（rc.8 走 "route B：pnpm
   hoisted"）。npm 建的新树比它：字节差 3.0%、**文件数差 14.4%**——安装器布局差异，不是缺陷。
   判"树可用"要靠**功能性证据**：ABI 门禁 PASS + 拿暂存树真起一次宿主（`vendor-equivalence --boot`）。
   另：`prunedBytes/prunedFiles` 为 0 只说明那是 `--prune-only` 在已剪过的树上跑的，不代表没剪枝。
19. **断言命名会污染门禁**（S2 实证）：把断言名写成 `FAIL 带上了具体原因`，输出行就是
   `  PASS  FAIL 带上了具体原因`——任何 `grep FAIL` 的 CI 都会把全绿读成失败。**断言名不得以
   `FAIL`/`PASS` 开头**；同理，门禁脚本自身的输出格式就是它的对外契约。
> 更全的坑与规范见仓库根《代码规范与范例.md》（第 0 节新会话开工清单 + 第 4 节坑清单），两者保持一致，新坑两边都要回填。

## 四、DSH 侧规则（零改动承诺的边界）

1. **不改 DSH 源码**；壳侧一切走 `--port`/`--patch`/admin API/环境变量。
2. **patch 按行 id 定位，config 是整体替换语义**：改某行 config 必须带全该行全部键（如 web-runtime 的 trustedHosts/surfaceContext）。
3. **bundles 锁精确版本**（0.1.0-rc.6）；升级走 build-host 全量重建 + 双冒烟门禁。
4. profile 的模块解析是"双锚"机制，与包管理器无关（pnpm 只在 `dsh plugin` CLI 场景需要）。
5. 宿主入口复用 dsh 包自带 `lib/bin.js`，不自行重写 runProfile 编排。
6. **loader 对插件条目做 ESM 解析的基准是 profile 目录本身**（`$DSH_HOME/profiles/web/`），自研插件包必须落到 out-of-tree 插件位 `profiles/web/node_modules/<包名>`（`dsh plugin` 机制的落点）——vendor/profile 里的副本不会被父级解析覆盖到（ERR_MODULE_NOT_FOUND 实证）。壳启动时幂等同步（ensureProfilePlugin）。

## 五、命名规则

| 域 | 规则 | 实例 |
|---|---|---|
| 文件 | kebab-case，小写连字符 | `main.mjs` `host.mjs` `build-host.mjs` `boot-smoke.mjs` `abi-scan.mjs` `desktop.patch.yml` |
| 目录 | 职责单一：`src/`（壳代码）、`scripts/`（构建/门禁）、`vendor/profile`（自包含运行时）、`build/`（打包资源）、`dist/`（产物） | — |
| 环境变量 | 统一 `DSH_` 前缀，作用即名 | `DSH_HOME` `DSH_WS` `DSH_BIN` `DSH_APP_DATA` `DSH_SMOKE` |
| admin API | `/api/<小写名词>`，与 v1 契约一致 | `/api/status` `/api/workspace` `/api/quit` |
| 测试目录 | `.dsh-m*` / `.dsh-pkg*`（gitignore 覆盖） | `.dsh-m0` `.dsh-pkg-home` |
| 日志 | `app.log`/`host.log`，轮转 `名.log.YYYYMMDD`，留 7 天 | `host.log.20260818` |
| commit | `<阶段前缀>: <中文动作>（关键数字）` | `M0: deps（electron 43.4.0 ...）` `fix: findDshBin 锚点错位` |
| 版本 | 壳独立 semver + 根 `VERSION` 文件 + CHANGELOG 条目；DSH 依赖锁 rc 版本进 vendor.lock.json | `0.4.0` |
| 门禁脚本 | 幂等、可重复、有 exit code；输出 `PASS/FAIL` 行 | smoke.mjs、abi-scan.mjs |

## 六、门禁顺序（改动任何东西后）

```
node --check <改动的.mjs>          # 语法
node scripts/update-self-test.mjs        # 更新发现 29 断言（脱网）
node scripts/vendor-build-self-test.mjs  # vendor 构建原语 49 断言（脱网、不跑真 npm）
node scripts/repair-self-test.mjs  # repair 自愈 8 断言
node scripts/admin-bg-test.mjs     # admin 背景图 7 断言
node scripts/build-host.mjs --prune-only   # vendor 变了才需要（含插件同步 + ABI 门禁）
node scripts/boot-smoke.mjs ...    # 宿主链路（无 GUI 也能跑）
node scripts/smoke.mjs             # 壳全量 40 断言（需交互会话/完整权限）
node scripts/vendor-equivalence.mjs --boot   # 暂存树验证（重，分钟级，换树前才跑）
electron-builder + win-unpacked --smoke   # 打包产物（发版前必跑）
```

不 commit 不进下一阶段；检查点失败 = 阶段未完成，不"口头通过"。
