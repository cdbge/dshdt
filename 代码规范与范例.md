# DSH 桌面化项目 · 代码规范与范例（供 AI 会话参考）

> 本文的用途：**给后续对话（换新会话、新上下文）当项目记忆**。每次开工先读本文和
> 《Electron施工计划与进度.md》（唯一进度事实源），按本文的约束与范例写代码。
> 维护义务：本文随项目演进更新；新增的坑必须回填第 4 节。

---

## 0. 新会话开工清单（无缝衔接协议）

**当前状态锚点（2026-09-11 深夜，DSH 更新按钮事故修复之后）**：版本 **0.4.6**（用户指示：**未经允许不推进版本号**）。已热更新到本机已装应用两次：先装 DSH 更新按钮（备份 `app.asar.bak-0.4.6-dshupdate`），再装事故修复（备份 `app.asar.bak-0.4.6-dshfix` + `desktop.patch.yml.bak-0.4.6-dshfix`）。**已装应用 = 含双门禁与回滚的 asar + harness rc.8 vendor + 两个自带插件**；已装 `--smoke` 实测 **SMOKE OK**。未打安装包，dist 仍只有 `DSHDesktop-Setup-0.4.5.exe`（**它内含 9/8 时代的 vendor，重装会把 harness 退回旧版——别把它当修复手段**）。门禁基线：语法 OK / update 36 / vendor-build 54 / dsh-apply 44 / repair 8 / admin-bg 7（**离线合计 149**）/ smoke **49/49**。DSH 依赖锁 **0.1.0-rc.8**。**跨版本升级默认拒绝**（`assessJump`），需显式 `allowUnsafeJump`。**新增能力时的第一条判断：它会被打包态调用吗？会的话不能放 `scripts/`（坑 27）。**

新会话开工顺序（照做即可无缝衔接）：

1. 读本文（尤其第 1 节铁律与第 4 节坑清单）→ 读《Electron施工计划与进度.md》的"进度快照"表最后一行，确认锚点没变。
2. 环境自检：`git -C D:\Desktop\deepseek log --oneline -3`（干净工作树）→ 确认已装应用版本（`http://127.0.0.1:25439/api/status` 的 version/webUrl 字段，端口每次重启会变）。
3. 跑基线（幂等，全绿才算环境没坏）：`node scripts\smoke.mjs`（需完整权限，**49** 断言）。
   沙箱下 node 的输出**不能经管道/重定向**（`| Select-Object`、`> $null`、`2>&1` 一律
   `拒绝访问`/`StandardOutputEncoding` 报错）——只能裸跑 `node scripts\x.mjs` 看退出码。
4. 任何改动：先跑相关检查点确认基线绿 → 改 → 重跑检查点 → 按第 5 节⑤同步全部文档 → commit。
5. 涉及**打包/asar 热更新/插件副本替换**：先征得用户同意（第 1.3 节铁律），绝不自行执行。
6. 会话内跑 electron 前：`Remove-Item Env:ELECTRON_RUN_AS_NODE`（第 4.2 节坑）。
7. 版本号：动手前确认 `VERSION` / `package.json` 与用户指定的一致；**未经用户允许不得推进版本号**（第 1.7 条铁律）。

---

## 1. 项目铁律（不可违背）

1. **DSH 零改动**：不修改 DSH（harness）源码。壳侧一切能力走 `--port` / `--patch` / 回环 admin API / 环境变量 / 浏览器 CSS 注入。自研插件只落在 `profiles/web/node_modules/<包名>`（out-of-tree 插件位）并经 patch `insert` 挂载。
2. **每次改动必须更新计划书**：《Electron施工计划与进度.md》的"进度快照"表与对应阶段小节；《DeepSeek-Harness桌面应用封装计划书.md》《Electron构建安装包计划书.md》按需补能力表/坑清单。**这是硬性要求，不是可选动作。**
3. **打包与更新必须先经用户同意（2026-09-08 起用户确认的硬规矩）**：
   - 跑 `electron-builder` 产出安装包（exe）→ **必须先征得用户同意与确认**，不得自行打包；
   - 对用户已装应用做 asar 热更新/替换 → 同样先征得同意；
   - 同意后打包仍须跑完第 4 条门禁；打包产物冒烟（`win-unpacked --smoke`）也要跑。
4. **门禁顺序**（改完任何东西后）：
   ```powershell
   node --check <改动文件>          # 语法
   node scripts/repair-self-test.mjs  # 会话日志自愈单测（动了 repair.mjs 才跑）
   node scripts/admin-bg-test.mjs     # admin 背景图单测（动了 admin.mjs 才跑）
   node scripts/smoke.mjs             # 全量 e2e 冒烟（任何壳代码改动必跑）
   # 发版才跑（且需用户同意）：build-host --prune-only（vendor 变了）→ electron-builder（打包）
   ```
   检查点失败 = 未完成，修复后重跑，**不"口头通过"**。
5. **不 commit 不进下一阶段**；commit message 带阶段/版本前缀（如 `fix(0.4.2): ...`）。
6. 行为契约先行：v1（`desktop-shell/` 已归档）与 Electron 分支的 admin API 面保持一致，smoke 断言是契约的执行者。
7. **版本号推进须经用户允许（2026-09-11 用户指示，硬规矩）**：未经用户明确同意，**不得**改动 `VERSION` / `package.json` / `CHANGELOG` 里的版本号，也不得据此命名迁移/备份目录与产物；功能照常开发、提交、更新文书，但**版本号保持用户指定的值**；要发新版必须先由用户点名。

## 2. 工程地图（desktop-electron/）

| 文件 | 职责 |
|---|---|
| `src/main.mjs` | 主进程：多窗口、host 复用编排（`.dsh-host.lock` + netstat 探测）、窗口/托盘/协议/退出（优雅停宿主）、背景图 CSS 注入（穿透 rc.6 不透明层）、admin 装配 |
| `src/node-guard.mjs` | 运行环境保护（main.mjs **第一个** import）：防 `ELECTRON_RUN_AS_NODE` 泄漏导致主进程退化成纯 Node |
| `src/early-errors.mjs` | 打包态未捕获异常落盘（main.mjs 第二个 import） |
| `src/host.mjs` | 模式 B 宿主托管：findDshBin / freePort / waitReady / killTree / startHost（RUN_AS_NODE + `--expose-internals` + `SSH_CONNECTION=dsh-desktop-browse` 钉住应用内目录浏览） |
| `src/dsh-update.mjs` | DSH 更新按钮 S1：版本**发现**（parseVersion / compareVersions / pickHighestVersion / readCurrentVersions / fetchPackument / checkForUpdate）。纯 Node、不写文件、不起进程 |
| `src/vendor-build.mjs` | DSH 更新按钮 S2：vendor 树**构建原语**（npm 探测 / install / 剪枝 / `syncVendorPlugins` / ABI 门禁 / lock / `buildStaging`）。**必须在 src/ 而非 scripts/——后者不进包**，见坑 27 |
| `src/dsh-apply.mjs` | DSH 更新按钮 S3：待应用标记（`pending-vendor.json`）、暂存树校验、`swapVendorTree` 换树、旧树清理。全同步、纯 Node。**换树唯一不可逆，每条失败分支都有单测** |
| `src/junction-safe.mjs` | junction 安全删除（`safeRemoveTree`）与遗留门禁目录清理（`cleanStaleBootGateHomes`）。**凡自己删可能含 junction 的目录都必须走它**，见坑 38 |
| `src/admin.mjs` | 回环 admin HTTP：`/api/*` + `/bg-image`（背景图供给）+ CORS（仅回环来源） |
| `src/repair.mjs` | 会话日志自愈：半个 zstd 尾帧截断 / 首帧异常逐行重编码 / 无法修复隔离改名 |
| `src/desktop.patch.yml` | 形态层 patch（web-runtime config 整体替换 + dsh-desktop-ui insert） |
| `packages/dsh-desktop-ui/` | 客户端插件：设置面板"桌面"section（`settings.section` 槽位）；按钮 ghost + hover/active 交互样式（`.dsh-desktop-btn` 注入样式表）；双副本同步路径见坑 13 |
| `scripts/` | 门禁与工具：smoke（**49** 断言）/ repair-self-test（8）/ admin-bg-test（7）/ update-self-test（29）/ vendor-build-self-test（49）/ dsh-apply-self-test（38）/ vendor-equivalence（暂存树验证，重）/ bg-probe2~5（背景图像素探针）/ hover-probe*（CDP 悬停实测）/ repro-picker-worker·parent-repro（选择器崩溃复现）/ gen-icon / boot-smoke；`build-host` 与 `abi-scan` 已退化为 CLI 薄封装（实现在 `src/vendor-build.mjs`） |
| `vendor/profile/` | 自包含 DSH 运行时（rc.6 锁定），build-host.mjs 生成，勿手改 |
| `build/icon.ico` | 唯一图标源（BrowserWindow/Tray/electron-builder 共用），由 `gen-icon.mjs` 从 workspace 根 `dsh.jpeg` 生成 |
| `$DSH_HOME/profiles/web/…` | HOME profile：`node_modules/dsh-desktop-ui`（壳启动幂等同步）+ `dsh-host-lock-registry`（纯登记 `.dsh-host.lock`，不拦截不退出）|

命名：文件 kebab-case；环境变量 `DSH_` 前缀（`DSH_HOME`/`DSH_WS`/`DSH_BIN`/`DSH_APP_DATA`/`DSH_SMOKE`）；admin API `/api/<小写名词>`；日志 `app.log`/`host.log` 按天轮转留 7 天；版本独立 semver + 根 `VERSION` 文件 + CHANGELOG 条目。

## 3. 代码风格

- **注释用中文**，模块头注释写"为什么"而非"是什么"；关键决策注释引用实证依据（如 #1452、评审稿编号）。
- **同步优先**：`fs` 用同步 API + try/catch（主进程小文件场景）；async 仅用于 fetch/等待/对话框。
- 函数小、单一职责；错误逐层 `log()` 不吞；`log()` 自己容忍目录不可写（console 兜底）。
- 所有副作用可逆：锁要释放、CSS 要 `removeInsertedCSS`、事件监听有 disposer。
- 状态文件/锁文件读写在模块内收敛为 `readState/writeState/readHostLock/writeHostLock/releaseHostLockIfOurs`，不散落。

## 4. 平台坑清单（本机/本项目实证，新坑必须回填）

1. **AI 会话沙箱**：Node `spawn` 用默认 pipe stdio 会 EPERM → 一律文件描述符重定向或 `stdio:'ignore'`；PowerShell 自己的管道不受影响。
2. **会话环境自带 `ELECTRON_RUN_AS_NODE=1`**（继承自桌面壳宿主的运行时）：在会话里跑 electron 前必须 `Remove-Item Env:ELECTRON_RUN_AS_NODE`，否则 electron.exe 退化成纯 Node，`import { app } from 'electron'` 报"无此导出"。
3. **Chromium 禁止 http 页面加载 `file://` 本地资源**（渲染器报 "Not allowed to load local resource"）：任何给 SPA 用的本地文件必须经壳的回环 HTTP 供给（背景图走 admin `/bg-image`，带 MIME + `Cache-Control: no-store` + `?t=mtime` 破缓存）。这是 0.4.1 背景图失效的根因。
3b. **"计算样式正确 ≠ 视觉可见"，UI 注入类改动必须像素级验证**（0.4.3 实测坑）：vendor 锁定的 rc.6 SPA 不用 `--dsw-*` 主题变量，而是用写死的 `rgb(21,21,23)` 不透明背景铺满视口（CSS-modules 类 `.*_frame`/`.*_root`）。0.4.2 的"body 铺图 + 变量透明"注入成功、图片加载成功、计算样式全对，但视觉上完全被盖住。修复 = 把真实不透明层置透明（`#root > div`、`[class$="_frame"]`、`[class$="_root"]`、`[class$="_centerCol"]`，`$=` 后缀定位随 rc.6 锁版本稳定；侧栏多类名 `*_quietBars` 天然避开、保持不透明）。验证手段：`scripts/bg-probe2~5.mjs`（elementsFromPoint + capturePage 像素对比，注入前后角落像素必须变化）。
3c. **rc.6 的 native 目录选择器会崩溃**（0.4.4 实测坑）：`dsh-host-directory-picker-native` 的 koffi COM worker 子进程在真实选取目录时硬崩溃、静默消失（无 stderr，host.log 为空），宿主报 "win32 folder dialog worker exited before reporting a result"。修复（DSH 零改动）：宿主子进程 env 注入 `SSH_CONNECTION=dsh-desktop-browse` —— auto 解析器读到即回退**应用内浏览**选择器（vendor 全树仅此一处读取该变量，语义安全）；别用"钉住 native"或 worker 相关方案。验证：smoke 断言 `host.pickDirectory`→`directory-picker-unavailable`（证明 browse 已钉住）+ `host.listDirectory`→ok。RPC 信封格式：`{type:'client-request', rpcId, method, payload}`（`server-request` 是宿主推送形态，别搞混）。
4. **打包态未捕获异常 = 无窗口静默挂起**（原生对话框卡死、无日志）：`early-errors.mjs` 必须最先导入。
5. **CJS 命名导入**：Node 24 的 ESM-CJS 互操作检测不到部分 CJS 包命名导出（如 electron-updater）→ `import pkg from 'pkg'; const { x } = pkg`。
6. **GUI 进程**：PowerShell `&` 不等待 GUI 程序返回（后台任务会"提前完成"，进程继续跑）；验证存活用状态文件/端口轮询。
7. **electron-builder**：`extraResources` 的 `from` 不能以 `node_modules` 为根（拷贝 filter 会整棵跳过）；`*.node` 与 `@img/**` 必须 asarUnpack。
8. **签名时间戳服务器**（digicert/microsoft 系）在本机网络不可达 → 带 CSC 环境变量的构建会 ETIMEDOUT 失败；无网络时构建**不带 CSC 变量出未签名包**（首次运行 SmartScreen 提示属预期）。
9. **netstat 解析 pid→监听端口**，不要依赖 WMI/CIM（可能被禁且慢）。
10. **DSH_HOME/.dsh-host.lock 语义**：锁文件在 home 目录里 → 天然证明"同 home"。壳只复用**本 home 锁指向的宿主**（锁内 pid 存活 → netstat 找端口 → 探测 `__DSH_BOOT__`）；**绝不采纳无锁候选**（HTTP 面无法验证对方 DSH_HOME）。host 侧登记靠 profile 里的 `dsh-host-lock-registry`（纯登记、不拦截、不退出）；壳自起宿主就绪后补写锁（含 port）。
10b. **会话日志 zstd 帧结构**：每帧独立可解码、首帧必须"恰好一行 session header"；强杀进程会留半个尾帧 → 启动前 repair（截断/重编码/隔离三档）。修改 repair 前先跑 `repair-self-test.mjs`。
11. **中文/空格路径**：给 Chromium 的 URL 一律 `pathToFileURL`（百分号编码）或经 HTTP 供给；`spawn` 参数用数组。
12. **dist 新旧共存易误发**：`dist/` 里多个版本 exe 并存时，发给别人前先确认文件名；发完新包后旧包可删，README 产物行同步。
13. **asar 热更新流程（已装应用免重装升级）**：asar 内含 `src/**`+`VERSION`+`package.json`+`node_modules/electron-updater`，**不能只从 workspace 源文件重打包**（会丢依赖）；正确做法 = `@electron/asar` extract 已装 app.asar → 覆写改动文件 → pack → 备份原文件后覆盖。注意：`dsh-desktop-ui/lib/client.js` **不在 asar 里**，它有两份副本（已装应用 `resources\vendor\profile\node_modules\dsh-desktop-ui\` 与 `$DSH_HOME\profiles\web\node_modules\dsh-desktop-ui\`），壳每次启动会从前者幂等同步到后者；热更新要两份都换。版本号显示读 asar 内 `VERSION`——只换插件不换 asar 时版本号不变，属预期。
14. **打包产物 --smoke 的退出码不可信**：在 AI 会话（沙箱）里跑 `win-unpacked\DSH Desktop.exe --smoke`，即使全链路成功（日志出现 `SMOKE OK` + `cleanup: code=0`），进程退出码也可能是 `-2147483645`(0x80000003)——这是沙箱对 Electron 进程收尾的断点伪影，**以日志 SMOKE OK 为准，不以退出码判失败**；用户正常环境双击不受影响。
15. **CDP 悬停实测法（交互类改动验证）**：`scripts/hover-probe4.mjs` 为样板——探针窗口加载真实宿主 → 点 `button[aria-haspopup="dialog"]` 开设置面板 → **"桌面"是面板左侧导航页签（VOzbGW_navCell），必须先点它**，section 才渲染 → `webContents.debugger` attach + `Input.dispatchMouseEvent {type:'mouseMoved'}` 移动鼠标 → 对比 `getComputedStyle(btn).backgroundColor` 的 rest/hover 值。注意：隐藏窗口里 mouse-away 未必刷新 :hover，以 rest→hover 的变化为准。
16. **"桌面"section 按钮的交互样式**：ghost 透明基底 + `:hover`→`--dsw-alias-interactive-bg-hover`、`:active`→`--dsw-alias-interactive-bg-active`（官方同款 token；hover 规则必须 `!important` 才能压过行内样式）；样式由插件 apply() 注入 `<style>`（`ctx.effect` 回收）。

17. **harness 换树流程与 rc.8 差异（0.4.6 实证）**：vendor 是 `extraResources`（在 asar 外），换 harness **不必重打包壳**——
   退出应用 → 原地替换 `resources\vendor\profile` 内容（`robocopy /MIR`）→ 回拷 `dsh-desktop-ui`（它不在 npm 依赖里，
   是 build-host 第 3.5 步手工放入的）→ 刷新 `vendor.lock.json`。`$DSH_HOME\profiles\node_modules` 是**指向 vendor 的
   junction 场**，由 harness 的 `healProfilesModuleFallback` 在每次启动时自愈（幂等、会重指 moved installation、
   悬空链接对模块解析不可见）——所以换树不需要手工修链接。rc.8 把 markdown 渲染栈降为构建期依赖，换过去会出现
   **大量悬空链接（97 条）**，属预期。换树前必须先退出应用：宿主**映射着 vendor 里的 `*.node`**，用"能否独占打开
   `*.node`"判定比看进程名可靠（脚本 `deploy-vendor.ps1` / `stop-dsh.ps1`）。
18. **壁纸必须用 fixed 固定层，不能放在 body 背景上（0.4.6 实证）**：`html` 一旦有不透明底色（如 `#101216`），
   **body 的背景不会传播到 canvas**，未被 body 盒子覆盖的区域会露出平铺底色——用户报的"对话框底下那条黑条"
   就是这个。正确写法：`body::before { position: fixed; inset: 0; z-index: -1; background-image: … }`，
   配合把 SPA 的不透明层（`#root > div` / `[class$="_frame"]` / `[class$="_root"]` / `[class$="_centerCol"]`）置透明。
   验收口径：逐点比对"壁纸理论值（cover 映射 × scrim）"与实际像素，露底采样点数应为 0。
19. **亮度/模糊只能作用在壁纸层上**：`filter` 加在 `body` 会让正文一起被滤；必须加在壁纸固定层（`body::before`）上。
   模糊会让图层边缘发虚 → 固定层向外扩 2×模糊半径。调参经 `POST /api/settings` 落壳 settings，改完要 `reapplyBackground()`。
20. **本机 npm 的 cache 落在 `C:\Program Files\nodejs\node_cache`（普通用户不可写）** ⇒ 任何 `npm install` 直接
   `EPERM ... _cacache\tmp\...`。**已于 S2 修复**：`src/vendor-build.mjs` 的 `buildInstallEnv()` 现在
   **无条件覆盖** `npm_config_cache`（不再用 `process.env.npm_config_cache || <repo>\.npm-cache`——正是那个
   `||` 回退让外部已设的坏值顶掉了仓库内可写缓存）。`build-host` 仍可用 `--cache <目录>` 覆盖。
   另：`build-host` 一上来 `rmSync(vendor/profile)`，Windows 上可能因
   `.bin` 里的链接条目抛 EPERM → 先手工删掉该目录再重跑。
21. **像素探针三坑（0.4.6 踩齐）**：① 隐藏窗口不重绘 → 必须 `paintWhenInitiallyHidden: true` + 注入后等 ~2s +
   `invalidate()`，否则 `capturePage` 给陈旧帧（我曾据此误判"壁纸没变化"）；② 采样坐标要 **CSS 像素 × devicePixelRatio**
   （`capturePage` 给物理像素，本机 DPR=1.25），否则 `elementsFromPoint` 返回空数组；③ 跨源图会污染 canvas、
   `getImageData` 抛 SecurityError → 页内计算期望值必须用 **data URL**，并给 Promise 加超时兜底（否则永不 settle、探针挂死）。
   另外：探针默认把 JSON 写进仓库 `scripts/`，一律用 `PROBE_OUT` 指到工作区。
22. **覆盖主题 CSS 变量必须打在「最近的定义处」（0.4.6 实证，底部黑条根因）**：DSH 的主题别名定义在
   `body { --dsw-alias-bg-base: … }` 与 `body[data-ds-dark-theme] { --dsw-alias-bg-base:
   var(--dsw-static-neutral-bluish-950) /* #151517 */ }` 上。壳若只写 `:root { … !important }`，
   由于自定义属性取"最近的定义"，**body 会顶掉 html，覆盖形同不存在**——0.4.3~0.4.5 一直如此，
   表现为"注入成功但仍有一条实心 #151517 的黑条"。正确写法：
   `:root, body, body[data-ds-dark-theme], body[data-ds-light-theme] { --…: transparent !important }`。
   另：定位这类问题用 `POST /api/diag/opaque-layers`（真实窗口内列不透明层），比反复猜类名快得多。
23. **读不到截图时把图"量"出来**：模型无图像输入时，用 `System.Drawing` 逐行统计亮度/平坦度 +
   生成 ASCII 亮度图，足以判断版式与定位实心色带（0.4.6 就是靠它量到底部两条 11px/15px 的 `rgb(21,21,23)`）。

24. **写验收/补丁脚本时的两个"工具自身"陷阱（0.4.6 一夜误报两次）**：
   ① **PowerShell 变量名不区分大小写**：`$INST='D:\...\DSH Desktop'` 之后再写 `$inst = Join-Path $INST 'resources\app.asar'`，
   会把 `$INST` 覆盖成 asar 文件路径，后续 `Join-Path $INST 'resources\vendor\...'` 全拼错 → 误报"vendor 副本缺 client.js"。
   命名上就把"根目录"和"具体文件"分清（如 `$INST` / `$asarPath`）。
   ② **别用嵌套数组迭代 `@(@('a','p1'),@('b','p2'))`**：实测第一项会取错，同样导致误报缺文件；
   用 `[ordered]@{ 名 = 路径 }` 或显式两段。
   两条都说明同一件事：**验收脚本本身也要用"独立路径复算一遍"交叉验证**，否则会把脚本 bug 当成产品缺陷上报。

25. **写补丁脚本时的三个自伤陷阱（0.4.6 插件/托盘那一轮全踩，务必照这条做）**：
   ① **.NET 正则替换串里的 `$` 有特殊语义**：`$&`/`$1`/`` $` ``/`$'` 分别代表"整个匹配/捕获组/匹配前内容/匹配后内容"。
   我用 `[regex]::Replace($text, $block, 1)` 插入一段含 `\\s*$` 的代码（模板串里出现 `` $` ``），
   结果**把整个 main.mjs 复制了一遍**（791 → 1470 行）。**结论：插入代码一律用"按索引拼接"**
   （`$text.Substring(0, $m.Index) + $block + $text.Substring($m.Index + $m.Length)`），
   或给替换串里的每个 `$` 加倍。`String.Replace`（字面量）没有这个问题。
   ② **多行锚点会被行尾坑死**：工作区文件是 LF，而脚本里 here-string 的换行取决于脚本自身编码；
   一旦不一致，多行锚点命中 0 次。**只用单行锚点 + 插入前先探测该文件的行尾**（`\r\n` 还是 `\n`）。
   ③ **改名/重构后必须全局搜调用点**：`ensureProfilePlugin` → `ensureProfilePlugins` 漏改一处调用点，
   smoke 立刻 `ReferenceError`（若当时直接热更，壳就起不来了）。**改完先 `node --check`，再跑全量冒烟**。
   ④ 收尾动作：任何"批量改仓库"的脚本都要在执行前 `git status` 留底、失败后 `git checkout -- <file>` 回滚，
   并**核对行数**（本夜就是靠"791 → 1470"立刻发现被复制）。

26. **asar 热替换后必须重启应用（0.4.6 实测）**：Electron 对 `app.asar` 的目录索引是**首次访问时缓存**的。
   把 asar 换掉而进程不重启，运行中的主进程会按**旧索引**去**新文件**里读，拿到错位数据。
   实测现象：`/api/status` 的 `version` 变成 `ositio`——那是 `readVersion()` 读 `app.asar` 内 `VERSION`
   时按旧偏移取到的垃圾（`VERSION` 恰好 6 字节，所以读出来是 6 个字符的碎片）。
   ⇒ 结论：**asar 热更新 = 落盘 + 重启应用**，两步缺一不可；期间不要相信运行中进程读到的 asar 内容。
   仅替换 `resources/vendor/**`（asar 外）或 profile 插件位不受此影响，可用托盘「重启宿主（重载插件）」生效。

27. **`scripts/` 不进包，门禁代码必须落在 `src/`（S2 实证）**：`electron-builder.yml` 的 `files` 只有
   `src/**` + `VERSION` + `package.json`。原来 `abi-scan.mjs` 在 `scripts/`，**打包后根本不存在**；
   而装好的应用要能重建 vendor 树、且门禁是唯一防线（无回滚备份）。⇒ 安装/剪枝/插件同步/ABI 门禁
   全部搬进 `src/vendor-build.mjs`，`scripts/build-host.mjs` 与 `scripts/abi-scan.mjs` 退化成 CLI 薄封装，
   共用同一份实现（这也顺带根治了"两处各写一遍构建逻辑"的漂移风险）。**新增 shell 侧能力时先问一句：
   它会被打包态调用吗？会的话就不能放 `scripts/`。**

28. **`vendor.lock.json` 的体积基线不是 npm 建的，别拿它当同构证据（S2 实证）**：现网 `vendor/profile`
   含 `node_modules/.pnpm`、`pnpm-lock.yaml`、`.modules.yaml` 且无任何 npm 锁文件——rc.8 换树走的是
   "route B：pnpm hoisted 安装"。拿 npm 建的新树去比它：字节数只差 3.0%，**文件数却差 14.4%**
   （13019 vs 15208）——这是两种安装器落盘布局差异的特征，**不是流水线缺陷**。
   ⇒ 判断"树可用"要靠**功能性证据**（ABI 门禁 PASS + 拿暂存树真起一次宿主的启动冒烟），
   体积对比只能当诊断输出。另：`vendor.lock.json` 里 `prunedBytes/prunedFiles` 为 0 说明那是
   `--prune-only` 在已剪过的树上跑的第二遍，不代表"没剪枝"。

29. **用 `null` 兼表"未初始化"与"无约束"，守卫会静默失效（S5 抓到的真漏洞）**：DSH 更新里
   "只接受检查更新查证过的版本"的守卫原本写成 `if (target !== null && version !== target) return 拒绝`。
   而 `target === null` 的真实含义是**从未检查过更新**，不是"版本随便填"——于是任意版本串都能绕过守卫
   并启动构建（把用户输入拼进 npm 依赖 = 供应链面）。正确写法是先判 `target === null` 直接拒绝。
   ⇒ 通用规则：**一个哨兵值不要同时承担"还没初始化"和"没有限制"两种语义**；守卫要显式区分，
   并且给"未初始化"分支单独写一条断言（本条的漏洞就是被 smoke 的拒绝路径断言逼出来的）。

30. **断言要打在正确的对象上——`waitState()` 读的是状态文件，不是 HTTP 响应（S5 踩）**：
   `smoke.mjs` 的 `st` 来自 `waitState()`，那是壳写给自己做重启/宿主复用的 `app.state.json`；
   `/api/status` 的 HTTP 响应是另一个对象（`s1.json`）。两者字段集不同，打错对象会得到"字段不存在"
   的假失败。⇒ 断言前先确认这个变量的**来源**；UI 依赖的字段一定断言 HTTP 那一份。
   （同类错误在本项目已出现三次：夹具填错值、断言名污染 grep、断言对象打错。规范 §24 值得反复读。）

31. **门禁必须包含"功能性"证据——ABI 门禁不覆盖"宿主能否对外服务"（0.4.6 事故根因）**：
   DSH 更新按钮上线时只接了 ABI 门禁（逐个 dlopen `.node`）。它对一棵**根本起不来**的树给出
   `OK=5 SKIP=0 FAIL=0` 全绿——因为它压根不关心 HTTP 能不能通。计划书 §8 原本写的是"ABI 门禁 +
   启动冒烟双绿才发 marker"，实现时漏了后者，于是按钮发货时没有安全网。
   ⇒ 规则：**任何"换掉运行时"的路径，门禁必须真起一次并探到就绪**；"能加载" ≠ "能用"。
   落地：`vendor-build.mjs` 的 `runBootGate`，且它复用 `host.mjs` 的 `startHost`/`extractHostUrl`——
   **门禁与壳必须走同一套启动与就绪判定**，否则会放行"门禁里能起、壳里起不来"的树。

32. **壳与宿主子进程之间，stdout 是唯一的信息通道；关掉它等于自断生路（0.4.6 事故第二因）**：
   `desktop.patch.yml` 曾把 `printUrl` 设为 `false`（图日志干净）。而 DSH 0.1.5 起根 URL 带**进程级
   启动令牌**（`?token=…`），壳是独立进程、够不到宿主的 cordis 上下文——那行 stdout 是唯一能拿到
   带 token URL 的地方。关掉它 → 壳只能探裸 URL → 永远不就绪 → "进程活着但前端拉不起来"，
   而报错只有一句笼统的超时。⇒ 改为 `printUrl: true` + `extractHostUrl()` 解析。
   任何"为了让输出干净"而砍掉子进程输出通道的改动，先问：**这是不是唯一通道？**

33. **跨版本升级守卫必须守对"版本位"，且必须先拿真实事故样本验一遍（0.4.6 事故第三因）**：
   `assessJump` 第一版只比 major/minor，于是把真实事故 `0.1.0-rc.8 → 0.1.5-rc.2` 判成**安全**——
   等于完全没挡住。本项目 harness 的**修订位才是真正的发布轴**（`0.1.0` 后面直接跟 `0.1.5`）。
   ⇒ 口径：只放行"仅预发布号变化"（`rc.8 → rc.10`），major/minor/patch 任一位变化都要显式确认。
   **通用教训：守卫写完后，先用"已知的真实事故样本"跑一条断言**——否则守卫可能只是看起来像守卫。
   配套：换树后若宿主不就绪，必须有 `restoreOldTree` 兜底换回（失败发生在换树**之后**，
   "延迟删旧树"救不了）。

34. **`desktop.patch.yml` 是双份的，打包态读的是 extraResources 那一份**：它既在 asar 内
   （`src/desktop.patch.yml`，`files: src/**` 收进去）又在 `extraResources`
   （`resources/desktop.patch.yml`）。而 `main.mjs` 的 `PATCH_FILE = app.isPackaged ?
   path.join(RES,'desktop.patch.yml') : …`——**打包态读的是后者**。热更新只换 asar 会漏掉它，
   症状是"改了补丁但行为没变"。⇒ 改 `desktop.patch.yml` 时两份都要落。

35. **启动门禁测不出"与用户 `$DSH_HOME` 现有状态不兼容"——那要靠换树后的兜底回滚接住**
   （0.4.6 事故第二轮）：`runBootGate` 必须用**隔离 DSH_HOME**（否则会污染用户真实状态、写会话），
   于是它只能证明"这棵树本身起得来"，**证明不了"它与用户现有 `.credentials.yaml`/settings/sessions
   合得来"**。本轮的真凶正是这一格：0.1.5 把 `$DSH_HOME/.credentials.yaml` 改写成嵌套格式
   （`version: 1` 是**数字**），rc.8 的解析器要求扁平「凭证名 → 非空字符串」→ 抛 TypeError →
   宿主 `code=1` → 前端起不来。**且该文件在 `$DSH_HOME`，卸载程序不删 → 重装无效。**
   ⇒ 结论：门禁覆盖不到的那一格，必须由 `restoreOldTree` 兜底；而兜底要能触发，见坑 36。

36. **失败路径用 `return` 而不是 `throw`，会让所有"出错就回滚"的兜底静默失效**（0.4.6 事故根因）：
   `bootHost()` 原写作 `if (url === null) return`。后果有两层：① `main()` 接着拿 `null` 去
   `loadURL`，Electron 抛 `conversion failure from null`——**一句与真因毫无关系的报错**；
   ② 换树兜底回滚只在 `bootHost` **抛错**时触发，返回 null 不触发，所以**回滚代码写了却从未运行**。
   ⇒ 规则：**"没能完成任务"必须走异常路径**；静默 `return` 等于把故障藏起来，还会连带废掉所有
   基于异常的兜底。同理：**子进程非零退出时要把它的日志尾巴带出来**——只报一个 `code=1`
   会把排查引向完全错误的方向（本轮因此多绕一整轮）。

37. **给日志/密钥打码时，匹配"值的形态"而不是"键的位置"**（0.4.6 事故的安全教训）：
   用 `^(KEY):\s*(.+)$` 这种**行首锚点**打码，遇到 `  DEEPSEEK_API_KEY: sk-…`（缩进两格）
   会全部落空，把真实密钥原样打进会话记录。⇒ 按值的长短/字符集匹配（或压根不打印取值，
   只打印长度），并**先在一条真实样本上验证打码规则真的命中**。凭据一旦外泄只能吊销重发。

38. **`profiles/node_modules` 的 junction 场是"删目录"动作的哑雷（0.4.6 事故第二现场）**：
   harness 的「profile 模块解析双锚」会在 `$DSH_HOME\profiles\node_modules` 下建**近 200 个 junction**
   指向 `resources\vendor\profile\node_modules\*`（含一个指向整个 `@deepseek-ai` 的）。启动门禁跑隔离
   HOME 时也会造出同样一套、指向**被测树**。
   ⇒ **危险不在 Node**：实测 `fs.rmSync(dir,{recursive:true})` **不跟随** junction（最小复现：目标 3 文件，
   删后仍是 3）。危险在**别人怎么删这个目录**——Windows 上 `rmdir /s /q` 与 `del /s /q` 会跟随。
   实测到的损坏形态极具辨识度：`vendor\staging\<ver>\profile` 里 **240 个 `@deepseek-ai/*` 包被掏空成
   空目录**（连 `package.json` 都没了）、文件数 11000→4000。**"目录还在、文件全没"就是 `del /s /q`
   的特征**（只删文件不删目录），而一次这样的删除经过 `@deepseek-ai` 那条 junction 就能一次性掏空全部
   240 个。处置：**凡我们自己删可能含 junction 的目录，一律走 `src/junction-safe.mjs` 的
   `safeRemoveTree()`**（逐个 unlink 链接本身，绝不递归进目标）；壳每次启动再
   `cleanStaleBootGateHomes()` 收掉上次没走完 `finally` 的隔离目录。判年龄要用 **birthtime 而非 mtime**
   ——目录内容一变 mtime 就刷新，会把"正在跑的门禁"误判成遗留（写这条单测时正是被自己坑了一次）。
   注：本条是**强线索而非已证实的结论**——坏树被排查过程自己删掉了，无法再复现取证。

39. **就绪探测不能只认 `res.ok`：0.1.5 的根 URL 是 303 换 cookie，而 Node 的 fetch 没有 cookie jar**：
   实测三代形态——裸 URL → `401 dsh web authentication required`；**带 token + 默认跟随重定向 → 401**
   （undici 在 303 跳转时不带 `Set-Cookie`）；带 token + `redirect:'manual'` → `303 + Set-Cookie: dsh-auth-…`
   → **带上该 cookie 请求 `/` 才是 200**（实测拿到 28029 字节完整 SPA HTML）。而 `runBootGate` 与壳的
   `waitReady` 都写的是 `if (res.ok)`，于是对**任何** 0.1.5+ 的树都永远探不到就绪 → 与"宿主活着、日志
   在滚、前端永远拉不起来"的原始事故症状完全一致（第一轮只加了"token 感知的 URL 提取"，方向对了一半，
   没解决换票这一跳）。
   ⇒ 两处判据要分开：**门禁**做完整 303→接 cookie→200 握手（它起的是用完即杀的宿主，可以消费 token）；
   **壳的 `waitReady` 只要求"服务器回了任何 HTTP 响应"**，然后把带 token 的 URL 原样交给窗口——
   窗口有真正的 cookie jar 会自己走完换票，且避免探针先把一次性 token 消费掉。
   **已实施**（`host.mjs` 的 `probeHostReady` + `waitReady` 改用 `redirect:'manual'`）并回归验证：
   拿真实的 0.1.5-rc.2 事故树跑修好的门禁，**从 90s 超时失败变为 5.8s PASS**。

40. **守卫必须给用户一个"能完成的动作"——否则它不是守卫，是死锁（本轮用户实测反馈："什么叫拒绝更新"）**：
   跨版本守卫 `assessJump` 拒绝时返回的是 `拒绝升级：…确认要跨版本升级请带 allowUnsafeJump: true`。
   这句话**面向的是写代码的人**：界面那个「更新」按钮只发 `{}`，永远不会带这个参数，
   于是**更新在 GUI 里被永久卡死**，用户看到的只有一句莫名其妙的"拒绝更新"。
   ⇒ 规则：**任何"需要人确认"的守卫，确认入口必须同时落到界面上**。做法是把评估结果
   （`jump`/`needsConfirm`）放进只读快照让客户端能渲染，客户端在用户确认后代传那个参数；
   **不要因为 `needsConfirm` 就把按钮禁掉**——禁用等于没有确认入口。报错文案要写"你该做什么"，
   而不是"你该传什么参数"。**推广：凡是"拒绝 + 要求某个条件"，都要问一句"这个条件用户做得到吗"。**

41. **"它答了话"不是就绪判据——401 也是"答了话"，而它恰恰说明还不能用**（0.4.6 鉴权页实测）：
   坑 39 把壳的 `waitReady` 改成"任何 HTTP 响应即就绪"（`if (res.status > 0) return url`），意图正确，
   但**漏了时序**：轮询**早期**宿主还没把带 token 的 URL 行写进 `host.log`，`extractHostUrl` 返回 null，
   此时候选列表退化成裸端口 URL——而裸 URL 对未鉴权请求回 **401**，`status > 0` 成立，
   **当场定稿**返回裸 URL → 窗口加载无票地址 → 页面停在 `dsh web authentication required`。
   症状极具误导性：**宿主明明起来了、日志正常、`ready` 行也打了**，只有窗口是坏的。
   ⇒ 判据要分两段：**宿主已宣告本端口 URL 时只认它**（此时任何 HTTP 码都表示在服务，
   303 交给窗口换票）；**尚未宣告、只能探裸 URL 时只接受 2xx**。实测修复后
   `ready: http://127.0.0.1:9712/?token=…`（带票，耗时从 ~1s 变 ~5s——那 4 秒正是早期 401 被正确
   拒绝、一直等到宿主宣告 URL 的时间，是**对的行为**，不要为了"快"把它改回去）。

42. **皮肤类注入的选择器只能用 CSS-modules 的"局部名后缀"，绝不能用全名**（0.4.6 皮肤实测）：
   0.1.5 的类名形如 `eGxaPq_marks` / `wSkVaW_widthHandle` / `pI_x6G_handle`——**前缀哈希每次构建都会变**
   （换一次 harness 就全变），**后缀局部名才是跨版本稳定的**。项目原有的壁纸注入就是靠
   `[class$="_frame"]` / `[class$="_root"]` 这类后缀匹配，本次皮肤改造沿用同一手法：
   轮次标记轨 `[class$="_frame"]:has([class$="_marks"])`、会话根 `[class$="_root"]:has([class$="_scrollBody"])`、
   正文两侧拖动条 `[class$="_widthHandle"]`、
   滚动条走 theme 插件的全局 `::-webkit-scrollbar` + `--dsh-scrollbar-width`。
   **五条纪律**：① 后缀撞名时用 `:has()` 限定（`_frame` 与布局插件重名，靠 `:has(_marks)` 区分；
   插件自身也在用 `:has()`，可用）；② **⚠️ `:has()` 匹配的是"任意后代"，不是"直接子元素"**——
   这是本轮最严重的一次事故：标记轨位于布局根框架内部，于是
   `[class$="_frame"]:has([class$="_marks"])` **同时命中两个元素**（`pI_x6G_frame` = 整个对话页面，
   与 `eGxaPq_frame` = 标记轨），`opacity:0` + `translateY(-50%)` 把**整页隐藏并上移半屏**。
   ⇒ **用 `:has()` 时必须反问一句"这个选择器还可能命中哪些祖先？"，并用 `:not(:has(...))` 把
   已知的祖先排除**（此处排 `_centerCol`：布局根含它、标记轨没有）。
   ③ **"底层"遮罩要用 `z-index:-1`**：定位伪元素默认画在**内容之上**（会糊住正文），
   负层级才落在内容之下、页面底色之上；父元素还需是定位元素（`position:relative`）作基准。
   ④ **滚动条不要靠"宽度收 0"来隐藏**——那会让正文在悬停瞬间横向重排，抖动比"看不见"更难受；
   保留槽宽、只把滑块做成透明才是稳定的自动隐藏。
   ⑤ **改完必须在真实窗口里看**——`/api/diag/ui`（本轮新增的只读诊断口）只报结构，
   "注入成功但没效果"要按坑 3b 做像素级验证，别信计算样式。

43. **反引号模板字符串里不能出现反引号——注释里也不行**（0.4.6 自伤实测）：
   给注入的 CSS 写注释时用了 `` `:has()` ``（想标出函数名），而**反引号会直接终止正在书写的
   模板字符串** → `SyntaxError`。更糟的是**部署脚本没按 `node --check` 的退出码中止**，
   于是坏文件被写进了已装应用（下次重载就该插件整块失效）。
   ⇒ 两条：① 模板字符串内的注释一律**不用反引号**（用「」或直接裸写），也别写 `${`；
   ② **任何"检查后写入"的脚本里，检查失败必须 `exit`，不能只打印**——门禁的意义就在于拦住下一步。


## 5. 完整范例：新增一个壳设置项（端到端模板）

以"背景图片"为实例（已落地），新增任何类似能力按此四步 + 一步文档：

**① admin 路由（`src/admin.mjs`，纯 Node 可单测）**
```js
// 背景图片供给：Chromium 禁 http 页面加载 file://，本地图必须走回环 HTTP
if (req.method === 'GET' && u.pathname === '/bg-image') {
  const p = String((readSettings() || {}).backgroundImage || '')
  if (!p || !fs.existsSync(p)) return json(res, 404, { ok: false, error: 'no background image' })
  const data = fs.readFileSync(p)
  const mime = MIME_BY_EXT[path.extname(p).toLowerCase()] || 'application/octet-stream'
  res.writeHead(200, { 'Content-Type': mime, 'Content-Length': data.length, 'Cache-Control': 'no-store' })
  return res.end(data)
}
```

**② 主进程 action + 状态（`src/main.mjs`）**
```js
const BG_ALLOWED_EXT = ['.jpg', '.jpeg', '.png', '.webp', '.gif', '.bmp', '.avif', '.ico']
function setBackgroundImage(filePath) {
  if (filePath) {
    if (!fs.existsSync(filePath)) return { ok: false, error: '文件不存在' }
    if (!BG_ALLOWED_EXT.includes(path.extname(filePath).toLowerCase())) {
      return { ok: false, error: '不支持的图片格式，请使用 jpg/jpeg/png/webp 等' }
    }
    readSettingsWriteBackground(filePath)   // 持久化 settings.json
    applyBackgroundCss()                     // 注入/更新窗口 CSS
    return { ok: true, path: filePath }
  }
  clearBackground(); return { ok: true, cleared: true }
}
// 装配：createAdminServer({ ..., actions: { ..., setBackground: setBackgroundImage, pickBackground: pickBackgroundImage, quit: (code) => cleanup(code) } })
// 暴露：statusPayload() 增加 backgroundImage 字段（客户端 UI 依赖）
```

**③ 客户端设置行（`packages/dsh-desktop-ui/lib/client.js`）**
```js
// 桌面 section 内：label + 操作按钮；模态交互（文件对话框）不设超时
react.createElement("div", { style: css.row },
  react.createElement("div", { style: css.kv },
    react.createElement("span", { style: css.label }, "背景图片"),
    react.createElement("span", { style: css.hint }, st.backgroundImage || "未设置（建议选深色图片，文字更清晰）")),
  react.createElement("button", { style: css.button, onClick: async () => {
      const r = await post("/api/pick-background", {}, 0);   // timeoutMs=0：模态等待
      setMsg(r && r.ok ? (r.path ? "背景已更换" : "未选择图片") : "操作失败（壳未响应？）");
    } }, "浏览…"),
  react.createElement("button", { style: css.button, onClick: async () => setMsgOk(await post("/api/background", { path: "" })) }, "清除"))
```

**④ 冒烟断言（`scripts/smoke.mjs`，防回归）**
```js
const bgSet = await api(st.adminPort, '/api/background', { path: bgPng })
check('background 设置 (png)', bgSet.status === 200 && bgSet.json.ok === true)
const bgImg = await fetch(`http://127.0.0.1:${st.adminPort}/bg-image`, { signal: AbortSignal.timeout(5000) })
check('bg-image 供给', bgImg.status === 200 && (bgImg.headers.get('content-type') || '').includes('image/png'))
```

**⑤ 文档同步（同一次改动里完成，缺一不可）**
- `CHANGELOG.md`：新增版本条目（能力/修复/已知问题各一条）；
- 《Electron施工计划与进度.md》：进度快照表加行 + 阶段小节；
- 两份计划书：能力表/坑清单按需回填；
- `README.md`：能力表、结构注释、产物行同步；
- `VERSION`：版本号 bump（发版前）。

### 范例二：客户端按钮交互样式（ghost + hover/active，0.4.5 落地）

```js
// ① 行内基底：ghost 透明 + 边框（hover 高亮交给注入样式表）
const css = { button: { cursor: "pointer", background: "transparent", border, borderRadius: "8px", padding: "6px 12px" } }
// ② 按钮统一带 className
react.createElement("button", { style: css.button, className: "dsh-desktop-btn", onClick: ... }, "应用")
// ③ apply() 注入样式（ctx.effect 回收；hover 必须 !important 压行内样式）
const styleEl = document.createElement("style")
styleEl.textContent = `
  .dsh-desktop-btn { transition: background-color 0.15s ease, border-color 0.15s ease; }
  .dsh-desktop-btn:hover { background: var(--dsw-alias-interactive-bg-hover) !important; }
  .dsh-desktop-btn:active { background: var(--dsw-alias-interactive-bg-active) !important; }
  .dsh-desktop-btn:focus-visible { outline: 1px solid var(--dsw-alias-brand-primary, #3964fe); outline-offset: 1px; }
`
document.head.appendChild(styleEl)
ctx.effect(() => () => styleEl.remove(), "dsh-desktop-ui: button styles")
// ④ 验证：hover-probe4.mjs（CDP 鼠标移动实测 rest→hover 背景变化，见坑 15）
```

## 6. 检查点命令清单（恢复/验证用）

```powershell
Remove-Item Env:ELECTRON_RUN_AS_NODE -ErrorAction SilentlyContinue   # 会话内跑 electron 前必做
node --check src\main.mjs src\host.mjs src\admin.mjs src\repair.mjs src\dsh-update.mjs src\vendor-build.mjs src\dsh-apply.mjs packages\dsh-desktop-ui\lib\client.js
node scripts\update-self-test.mjs      # 版本发现 29 断言（脱网）
node scripts\vendor-build-self-test.mjs # vendor 构建原语 49 断言（脱网、不跑真 npm）
node scripts\dsh-apply-self-test.mjs   # 换树与标记状态机 44 断言（脱网）
node scripts\junction-safe-self-test.mjs # junction 安全删除 18 断言（脱网，建真 junction 验证目标不被掏空）
node scripts\repair-self-test.mjs      # repair 自愈 8 断言
node scripts\admin-bg-test.mjs         # admin 背景图 7 断言
node scripts\smoke.mjs                 # 全量 e2e（49 断言：admin 面 + 背景图 + browse 钉住 + 优雅退出 + DSH 更新面）
node scripts\build-host.mjs --prune-only   # vendor 插件同步 + ABI 门禁
node scripts\vendor-equivalence.mjs --boot # 暂存树验证（重，约 8 分钟，换树前才跑）
electron.exe scripts\gen-icon.mjs      # 从 workspace 根 dsh.jpeg 重建 build\icon.ico
electron.exe scripts\hover-probe4.mjs  # 悬停交互实测（需先配 PROBE_URL=当前宿主）
# 发版（需用户同意）：node node_modules\electron-builder\out\cli\cli.js --win nsis（无网络不带 CSC 环境变量）
# 打包产物冒烟：dist\win-unpacked\DSH Desktop.exe --smoke（隔离 DSH_APP_DATA/DSH_HOME；看日志 SMOKE OK，退出码不可信，见坑 14）
```

## 7. 提交规范

- 前缀：`M<阶段>:` / `fix(<版本>):` / `chore:`；中文动作 + 关键数字（如 `fix(0.4.2): 背景图改回环 HTTP 供给（file:// 被 Chromium 拒绝）`）。
- 一个 commit 一件事；commit 前跑完第 1.3 节门禁；commit 内容必须包含第 5 节⑤的文档同步。
