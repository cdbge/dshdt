# ============================================================
#  dshdt（DSH Desktop）—— 入库白名单思路：源码与文书入库，其余一律不入库
#  产物发布走 GitHub Releases，不进版本库（见 docs/通用/02-提交与门禁.md §5）
#  ⚠️ 本文件里 # 只在行首才是注释；行尾注释会让整行变成一个失配的模式（踩过，见项目坑清单）
# ============================================================

# ---- 依赖与包管理器缓存 ----
node_modules/
.npm-cache/
npm-cache/
.yarn/
.pnpm-store/

# ---- 工具链缓存（Electron 二进制 / electron-builder 的 NSIS 与签名工具）----
.electron-cache/
.electron-builder-cache/

# ---- 构建产物 ----
# dist/：安装包与 win-unpacked，发布走 GitHub Releases
desktop-electron/dist/
# vendor/profile/：build-host.mjs 构建期生成的自包含 DSH 运行时
desktop-electron/vendor/profile/
# vendor/staging/：换树用的临时暂存树（放同级是为保证 rename 同卷）
desktop-electron/vendor/staging/

# ---- 签名材料与凭据（永不入库）----
desktop-electron/scripts/certs/
*.pfx
*.p12
*.cer
*.key
*.pem
.env
.env.*

# ---- 测试隔离 home（冒烟/单测产物，后缀任意）----
.dsh-m*/
.dsh-pkg*/
.dsh-smoke-*/
.dshell-data/
.dsh-pkg-out.log
.dsh-pkg-err.log

# ---- 运行时与诊断产物 ----
# .dsh/：DSH 自己在这里落地项目级技能（如 skills/dsh-tool-catalog/SKILL.md）
.dsh/
# .dsh-inspect/：会话日志取证/修复的临时工作区（可能含凭证明文，禁止入库）
.dsh-inspect/
# testing/：早期手工测试留下的日志目录
testing/

# ---- 日志与探针输出 ----
*.log
*.log.*
desktop-electron/scripts/*-out.json

# ---- 本地参考资料与草稿（不入库）----
# cankao*.md：临时参考的别家 README / 草稿，不属于本项目内容
cankao*.md

# ---- 编辑器与操作系统 ----
.vscode/
.idea/
Thumbs.db
Desktop.ini
.DS_Store
