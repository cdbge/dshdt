# 新会话开工清单（项目状态锚点 + 交接便条）

> **本文是「活页」**：每次收工都要重写；其他项目文书是「死页」，尽量少动。
> 通用写法（交接便条模板、什么算「好锚点」、开工顺序模板）见 `docs/通用/03-AI协作与文档义务.md` §2/§3。
> 进度原始记录在 `docs/项目/计划/Electron施工计划与进度.md`（**唯一进度事实源**），本文只保留「接手所需的当前态」。
> 文档归属：本文原为《代码规范与范例.md》第 0 节，2026-09-13 文书重构时独立成篇（对照表见 `docs/项目/00-文档导航.md` §3）。

## 0. 新会话开工清单（无缝衔接协议）

**当前状态锚点（2026-09-13 收工：仓库整理 + 文书重构 + GitHub 上传前准备）**：版本仍 **0.4.6**（用户指示：**未经允许不推进版本号**），**应用与代码零改动** —— 本轮只动文书与仓库卫生，`desktop-electron/src/**` 与 `packages/**` **逐字节未变**；工作树干净，跟踪文件 **56** 个。**✅ 清理（用户选 B 档）**：删除 **1,122 MB** 冗余（0.4.5 旧包 exe/zip/blockmap、0.4.6 的两份 `.bak` 与重复 zip、`dist\.cache`、根 `.npm-cache`、`.dsh-inspect\`、`testing\`），并 `git rm` 14 个一次性探针脚本（bg-probe1~5 / hover-probe1~4 / rail-probe / dsh15-probe / dsh15-cookie-probe / parent-repro / repro-picker-worker）+ 2 个无用的 `.gitkeep`。**⚠️ 口径变化：`dist\` 里现在只剩 0.4.6 一个包**（0.4.5 已删，「误发旧包」风险随坑 12 一起解除）。**✅ 文书重构**：原《代码规范与范例.md》《开发注意事项与命名规则.md》拆成 **`docs/通用/` 4 份（可整目录复用到任何项目）+ `docs/项目/` 5 份 + `docs/项目/计划/` 4 份**，**坑编号 1~59 未变**（`见坑 N` 全部继续有效），旧→新映射表在 `docs/项目/00-文档导航.md` §3；新增**坑 60**（`.gitignore` 行尾注释陷阱）。**✅ GitHub 准备**：新增 MIT `LICENSE`、`.gitattributes`（库内 LF / Windows 脚本 CRLF）、README 重写为对外入口。**✅ 已推送 GitHub（2026-09-13 当日完成）**：**https://github.com/cdbge/dshdt**（public / MIT / `main` / 74 提交，远端 HEAD 与本地一致，工作树干净）。过程要点：① 网络本来是通的，坏的是**本机 schannel TLS 后端**（`SEC_E_NO_CREDENTIALS`）→ `git -c http.sslBackend=openssl` 即通；② 沙箱下 **git 的 `sh.exe` 起不来**（信号管道被拦），故 shell 形式的 credential helper 不可用 → 从 GCM 取凭据**一次性注入**；③ `origin` 配置里**不含任何 token**。**唯一剩余动作：把 0.4.6 安装包作为 Release 附件上传**（产物不入库）。门禁基线未变：**9 套离线自检合计 242 断言**（8 套纯逻辑 = 235 + `admin-bg` 7）、smoke **72/72**。

### 交接便条（2026-09-13 收工 · 次日照此续接）

**一句话现状**：**代码与应用都没动**（0.4.6 仍是当前版本，没有"等重启"的尾巴）；本轮只做三件事 —— **清掉 1.1 GB 冗余**、**把文书改成「通用 / 项目」两层（通用 4 份可直接给以后任何项目复用）**、**把仓库整理到"一条命令即可推送 GitHub"**（缺的只有网络与远端仓库）。

**本轮做完的事**（决策与细节见 `docs/项目/计划/Electron施工计划与进度.md` 进度表最后一行、`CHANGELOG.md`）：

1. **清理**：删 1,122 MB —— 0.4.5 旧包 exe+zip+blockmap（257 MB）、0.4.6 两份 `.bak`（254 MB）与重复 zip（126 MB）、`dist\.cache`（215 MB）、根 `.npm-cache`（253 MB）、`.dsh-inspect\`（14.6 MB，**内含 `preupdate-*\.credentials.yaml` 凭证明文快照**）、`testing\`；`git rm` 14 个排障脚本。
   **刻意保留**：`desktop-electron\.npm-cache`（`build-host.mjs` 的离线依赖缓存 —— 本机无网时它是重建 vendor 的唯一依靠）、`.electron-cache` / `.electron-builder-cache`（离线打包工具链）、`dist\DSHDesktop-Setup-0.4.6.exe` + blockmap + `win-unpacked`、`scripts\certs`（本机自签，已 gitignore）。
2. **文书重构**：`docs/通用/` = 01 代码规范 / 02 提交与门禁 / 03 AI 协作与文档义务 / 04 排障方法与通用坑（**与项目无关，可整目录拷进新项目**）；`docs/项目/` = 00 导航（含对照表）/ 01 本文件 / 02 架构与铁律 / 03 坑清单 / 04 范例与检查点；四份计划书移入 `docs/项目/计划/`。
3. **GitHub 发布**：新建公开仓库并推送成功（**https://github.com/cdbge/dshdt**，74 提交）；MIT `LICENSE`、`.gitattributes`、`.gitignore` 重写为分区白名单式（每条注释独占一行）、README 重写为对外入口（是什么 / 快速开始 / 文档导航 / 已知限制 / 仓库与发布）。**敏感信息已核**：无密钥、无 token；作者姓名与邮箱按用户决定**保留现状**。
4. **本轮新坑（已回填坑 60）**：`.gitignore` 里 **行尾注释不是注释** —— `.dsh/   # 说明` 整行会变成一个失配的模式，于是 `git add -A` 把 `.dsh/skills/**` 与 `vendor/profile/{package,package-lock}.json` 一起提交了；发现后 `git rm --cached` + 修规则 + `commit --amend` 收拾干净。**判据**：`git check-ignore -v -- <路径>` 必须能命中。

**下一步（按需取用）**：

1. **发 Releases（唯一剩余动作）**：网页端 → Releases → Draft a new release → 新建 tag `v0.4.6` → 上传 `desktop-electron\dist\DSHDesktop-Setup-0.4.6.exe`（127.4 MB，未签名）。**不要提交进仓库**（`dist/` 已 gitignore）。日常推送直接 `git push`；若再遇 schannel 报错就加 `-c http.sslBackend=openssl`。
2. **发 Releases**：0.4.6 安装包（127.4 MB，**未签名**）走 Releases 上传，**不进仓库**（`dist/` 已 gitignore）；上传前按坑 12 核对文件名与体积 —— 现在 `dist\` 里只有 0.4.6，旧包已删。
3. **壳自更新（dshdt 自身）仍未启用**：`main.mjs` 的 `initUpdater()` 由 `HAS_UPDATE_SOURCE` 门控关闭；上传仓库后可接 GitHub 发布源，设计见《DSH更新按钮计划书.md》§11（用户口径："之后再说"）。
4. **其余"按需取用"项未变**（他人机器 `code=1` 的两类判据、验证"读命令"是否送达模型、自动放行的触发条件、三种微调方式）—— 见下方**上一轮交接便条**，本轮未触碰。

### 上一轮（2026-09-12 收工，存档备查）
**上一轮状态锚点（2026-09-12 收工，两棵树对齐 0.1.5-rc.2 + 0.4.6 安装包打出 + 宿主已重启验收）**：版本 **0.4.6**（用户指示：**未经允许不推进版本号**）。**✅ harness 版本已对齐：仓库 vendor 与已装应用现在都是 `0.1.5-rc.2`（各 240 个 `@deepseek-ai` 包）。** 本轮把 `build-host.mjs` 的 `VERSIONS` 从 rc.8 升了上来 —— 此前"两棵树不一致"（坑 45）**已消解**，皮肤选择器在两边通用。 已装应用 = 含双门禁与回滚的 asar + **0.1.5-rc.2** vendor + 两个自带插件，另含只读诊断 `POST /api/diag/ui` 与热加载 `POST /api/reload-window`。最近一次**具名**热更新备份是 **`app.asar.bak-0.4.6-imgver`**（09-11 23:50，`resources\app.asar.bak-*` 共 15 个）。⚠️ **asar 在 09-12 10:36 又被重打过一次，但没有建新备份** —— 已核对：其 `src\` **12 个文件与仓库逐字节一致**、`VERSION`=`0.4.6`，所以内容就是当前源码、无风险；但这说明"重打前先备份"这个环节漏了一拍，下次补上。**✅ 已装应用已完成整体重启**（用户重启过）：asar / vendor / 插件三样全部生效 —— `auto-approval.log` 最新装载记录 `2026-09-12T04:54:56Z plugin-loaded settings=ok commands=ok`，即"读命令"版插件 + `{global,prepend}` + `/approval` 命令现在都是活的。**✅ 安装包已产出**：`dist\DSHDesktop-Setup-0.4.6.exe`（127.4 MB，**未签名**），包内 vendor = 0.1.5-rc.2、两个内置插件齐全、产物冒烟 `SMOKE OK`（`cleanup: code=0`）。⚠️ **但 `dist\` 里 0.4.5 那个老包还在**（129.4 MB，内含 9/8 时代 vendor，**重装会把 harness 退回旧版**）—— 见坑 12，别误发、别拿它当修复手段。门禁基线：**9 套离线自检全绿、合计 242 断言**（`dsh-apply` 44 / `update` 36 / `junction-safe` 18 / `vendor-build` 66 / `repair` 8 / **`patch-mount` 16** / `admin-bg` 7 / **`dsh-auto-approval` 分级 10 + 接线 37**）——**口径**：其中 **8 套纯逻辑自检 = 235**，`admin-bg`（7）单列，故"235"与"242"都对，别当成对不上账；各自打印 ALL PASS（审批两套打印 `N passed, 0 failed`）且退出码 0；smoke **72/72**。仓库 DSH 依赖锁 **0.1.5-rc.2**（与已装应用**同一棵树**；`build-host.mjs` 的 `VERSIONS` 三包同进同退）。**跨版本升级默认拒绝**（`assessJump`），需显式 `allowUnsafeJump`。**新增能力时的第一条判断：它会被打包态调用吗？会的话不能放 `scripts/`（坑 27）。**

#### 上一轮交接便条（2026-09-12 收工）

**一句话现状**：仓库与已装应用**已在同一棵 harness 上**（`0.1.5-rc.2`，各 240 个 `@deepseek-ai` 包），
0.4.6 安装包**已打出并冒烟通过**，用户**已重启过应用** —— 本轮所有改动（壳 / 客户端插件 / 审批插件 / vendor）
**全部已生效，没有"等重启"的尾巴**。**✅ 他人机器"装完打不开"已定位并修复（同日）** —— 真凶是
**壳把插件挂载追加在 DSH 补丁层模板的 `[]` 之后**，造成同一文件两个 YAML 文档、宿主每次启动即 `code=1`
（**坑 59**，那台机器 36 次运行全是这一条，且卸载重装无效）。修法：挂载改为"末行是 `[]` 就替换、
否则追加" + 新增每次启动都跑的 `repairProfilePatchYaml()` 自愈；新增 `patch-mount-self-test`（16 断言），
并用**安装包载荷**喂朋友那份坏文件端到端验收（`SMOKE OK` / 9.3 秒）。
⚠️ **`dist\DSHDesktop-Setup-0.4.6.exe` 已于 09-12 17:29 重打（含补丁层修复），
旧 exe 备份为 `.bak-20260912-1728`；再前一次的备份是 `.bak-20260912-1418`。
打包产物冒烟（完整权限）`exit=0` / `stdio=pipe` / `SMOKE OK`**（铁律 1.3 的打包同意已获）。
**用安装包载荷 + 朋友那份真实坏文件做的端到端验收也通过**（壳日志"已修复损坏的 profile 补丁层" → 就绪 → `SMOKE OK`）。

**本轮做完的事**（按发生顺序，细节在《Electron施工计划与进度.md》进度表 20~34 行与坑 44~58）：

1. **界面打磨收尾**：对话区中央遮罩延伸覆盖两侧拖动滑块；修掉 `/api/diag/ui` 自身的手滑（坑 44）；
   右侧栏面板遮罩与对话区共用一档、按钮固定黑底；左侧栏背景三件套（模式 / 独立图片 / 遮罩，
   渲染值恒为 `max(设置值, 对话区遮罩)`）；右侧栏**全屏态单独一档**遮罩（默认 0.8）；
   修掉"已有图片时换图不生效"（URL 带 `?p=&t=`，坑 47）。**均已热更新 + 用户目视确认。**
2. **审批插件从"根本没生效"修到"真能自动放行"**：① 配置面换 schemastery（坑 48）；
   ② 确证**本体没有自动审批**这个功能（坑 49）；③ 裁决层 v2 —— 关键词表**降级为证据**，
   裁决权交给**一次独立模型调用**（走本体 `agent-default-model` + 统一 Key，插件零配置，fail-closed）；
   ④ 根因落定：**必须 `{ global: true, prepend: true }`**，否则 DSH 的审批桥先把请求拿走（坑 50）；
   ⑤ **让审查器"读命令"** —— 从 `tools/pre-execute` 按 `callId` 关联真实参数喂给模型。
   **实测**：两次真实裁决 `source=model`（理由都很准），一次**零点击 `auto-allowed`**。
3. **vendor 升到 0.1.5-rc.2 + 打出 0.4.6 安装包**：`VERSIONS` 三包同升 → 完整构建（install / 剪枝 /
   插件同步 / ABI 门禁 / 启动门禁 / lock 刷新）→ **240 包**；**smoke 迁移 4 处**（combo URL 供给、
   选择器改判名册、每次访问先换票、`restart-host` 单独放宽超时）→ **72/72**；`npm run dist` 产出
   **`dist\DSHDesktop-Setup-0.4.6.exe`**，产物 `--smoke` → `SMOKE OK`。坑 45 的"两棵树不一致"就此消解。
4. **他人机器"装完打不开"定位（文书线）**：朋友机装 0.4.6 后弹「DSH 宿主意外退出 exit code=1」→ 三次后应用退出。
   本轮**只做只读取证，不改任何代码**：① 用**包内那棵树**在隔离 `DSH_APP_DATA`/`DSH_HOME` 下跑 `--smoke`
   → **`SMOKE OK`、5 秒就绪**（⇒ 包是好的）；② 读源码把 `code=1` 的唯一来路钉死
   （`dsh-app-boot` 的 `installFailLoud` → stderr → 壳重定向进 `host.log`）；
   ③ 核实 **0.1.5-rc.2 会自动迁移旧版扁平凭证**（所以"旧凭证文件"不再必然致命）；
   ④ 一度得出"头号嫌疑 = 旧版残留 `DSH_HOME`"，**当日即被用户更正推翻**：**该机重装过 Windows，是全新环境**
   —— 旧数据理论不成立 ⇒ 病因此时**仍未定论**，转入"第 2 类：环境是否允许它跑"（杀软/组策略、
   `%USERPROFILE%` 含中文、全局 `NODE_OPTIONS`/`DSH_BIN`、解压不完整、N/KN 版），**等对方 `host.log` 收口**。
   产出：`desktop-electron/README.md` 新增「对方机器报『DSH 宿主意外退出 exit code=1』时怎么办」
   （**先分两类**：留旧数据的机器 vs 全新环境）+ 两张判据表 + `--doctor` 体检口径；
   `CHANGELOG.md` 0.4.6 小节；坑 **53~56**。**8 套离线门禁本轮复跑全绿**（219 + admin-bg 7；审批 10 / 37）。
5. **壳内加固 + 重新打包（本轮主线，用户指示"想个办法做出全新电脑能直接装的包"）**：
   先立目标（goal），再按用户点名做"重新打包 0.4.6"。**改动**：① `src/host.mjs` 宿主 stdio **两级化**
   （`spawnSync` 探管道 → pipe 优先 + 环形缓冲 + 保留遗言；EPERM 退化 fd 保功能）+ `stderrLogFile` 独立落盘；
   ② `src/main.mjs` 通知与壳日志带上"**最后遗言**"、新增 `--diag` 一键取证、新增**启动前 preflight**
   （与 `--doctor` 共用判据）、`VENDOR_EXPECT_FILES` 基线；③ `src/vendor-build.mjs` lock 增 `nodeModulesFiles`；
   ④ **未重建 vendor 树**（依赖仍 0.1.5-rc.2，锁未变），用 `electron-builder --dir` 从仓库 `src/` 重生成 asar，
   逐项核对 `electron-updater` 在包内、`src/` 与仓库逐字节一致。
   **验证**：8 套离线自检全绿 + **打包态冒烟（完整权限）** `exit=0` / `stdio=pipe` / `SMOKE OK` / `cleanup: code=0`。
   **自省（坑 58，重要）**：我一度在**受限沙箱**里连跑打包产物冒烟，把坑 1（pipe EPERM）与坑 14
   （沙箱退出码 `-2147483645` 是伪影、以日志为准）**两条规范里早已写明**的事实误判成"壳自杀"，
   白耗十几轮并据此做出错误推理 —— **打包态冒烟必须完整权限；动手前先读本文**。

**下一步（按需取用）**：

1. **`dist\` 现在只有 0.4.6 一个包**（0.4.5 与两份 `.bak` 已于 2026-09-13 清理删除）：`DSHDesktop-Setup-0.4.6.exe`（127.4 MB，**未签名**，含"profile 补丁层挂载修复"）+ blockmap + `win-unpacked`。要发人/传 Releases 直接用它；**重打才需要 `npm run dist`（需用户点头）**。旧备份如需追溯：`git log` 里没有安装包（`dist/` 从不入库），只能靠重新构建。
2. **别人机器报"DSH 宿主意外退出 exit code=1"**：**先分两类** —— ① 留着旧数据的机器（旧 dshdt / 恢复出厂保留用户目录）：
   先读 `host.log` 再**改名**（不是删）`%USERPROFILE%\.dsh` 与 `%LOCALAPPDATA%\DSHDesktop\dsh-home`，
   **别卸载重装**（重装碰不到那两个目录）；② **全新环境（Windows 重装过 / 新机器）**：旧数据理论不成立，
   查"环境是否允许它跑"（杀软与组策略、`%USERPROFILE%` 含中文、全局 `NODE_OPTIONS`/`DSH_BIN`、解压不完整、N/KN 版）。
   两张判据表与 `--doctor` 口径见 `desktop-electron/README.md`；机制与源码位置见坑 53。**当前病因仍未定论**
   （对方是全新环境，等 `host.log` 收口）。
3. **本机已装应用不必再装 0.4.6 安装包** —— 它和现在的已装应用是同一套东西（同 vendor、同插件），
   那个包主要是给"别人 / 新机器"用的。真要装，**先退出正在运行的 DSH Desktop**
   （安装程序拒绝覆盖在跑的 exe，见进度表第 17 行那次核查）。
4. **想验证"读命令"真的送到模型手里**：触发一次审批，看 `$DSH_HOME\logs\auto-approval.log` 的决策条目里
   `cmd` 字段是否非空（审计留 300 字预览，模型看 1200 字）。**若 `cmd` 是空串**，说明 `callId` 没关联上
   （审查器会退回"只读理由"，功能不崩但判断质量下降）—— 这是最值得盯的一条。
5. **自动放行的触发条件要理解对（别误判成 bug）**：当前会话是 `workspace-write`，**唯一能升的目标是
   `danger-full-access`**，一个称职的审查者**必然判 ask** —— 所以在当前配置下"省掉那几十次点击"
   **做不到**。要看到自动放行，得让请求本身有界（会话预设切「仅可查看」，此时请求 `workspace-write` → 放行；
   进度表第 30 行就是这么演示的）。
6. **签名 / 发布**：安装包**未签名**（构建日志里的 `signtool` 那几行是 electron-builder 的自签流程，
   不是正式证书）。EV 证书与 GitHub 发布源都没做，属"用户点头再动"。
7. 微调方式：改 `client.js` → `node --check` → **两份副本都换** → `POST /api/reload-window` → 刷新即见；
   改审批插件 `lib/index.js` → **必须重启宿主**（托盘「重启宿主（重载插件）」）；改 `src/*.mjs` →
   重打 asar + 重启应用（坑 26）。

**挂起 / 已由用户关闭的事项**：

- 「+」号子页面点击闪退 —— **用户已自行确认"我这里好了能用了"，明确说不用修**，不要再动它。
- **dshdt 壳自更新仍是关闭的**（`HAS_UPDATE_SOURCE` 门控）：2026-09-13 已完成"上传 GitHub 前的仓库准备"，但**仓库尚无 remote、本机推不上去**；推送完成后才谈得上接发布源（设计见《DSH更新按钮计划书.md》§11）。
- 左侧栏图片：`settings.json` 里 `sidebarBgImage` 被清空过（10:34），而 `sidebarBgMode` 仍为 `own`，
  所以侧栏现在没图 —— **等用户重新选一张**，不是 bug。
- **打包 / asar 热更新 / 插件副本替换仍须先经用户同意**（铁律 1.3）：本轮打 0.4.6 是用户点过头的，
  **不构成"以后可以自行打包"**。

**新会话开工顺序（照做即可无缝衔接）**：

1. 读 `docs/项目/00-文档导航.md` → 本文（状态锚点与交接便条）→ `docs/项目/02-架构与铁律.md`（铁律）与 `docs/项目/03-坑清单.md`（坑 1~60）→ `docs/项目/计划/Electron施工计划与进度.md` 的"进度快照"表最后一行，确认锚点没变。
2. 环境自检：`git -C <仓库根> log --oneline -3`（工作树应干净；2026-09-13 的收尾是"开源前清理"与"文书重构"两个 commit）
   → 确认已装应用版本：读 `http://127.0.0.1:<端口>/api/status` 的 `version` 字段。
   **端口每次重启都变**（本轮是 **8389**，更早是 25439）——从 `$DSH_HOME\.dsh-host.lock` 或壳日志里取当前值。
   `version` 必须读出可读的 **`0.4.6`**；若读出 `ositio` 之类乱码，说明壳还挂在旧 asar 索引上（坑 26）——
   让用户整体重启应用即可。**本轮已完成重启并核实**（asar / vendor / 插件三样全部生效），正常情况无需再动这一步。
3. 跑基线（幂等，全绿才算环境没坏）：先跑 **8 套纯逻辑自检**（`node desktop-electron\scripts\<x>-self-test.mjs`，
   秒级、可脱网；含新增的 `patch-mount-self-test.mjs` = 16 断言），再跑 `node desktop-electron\scripts\smoke.mjs`（**72** 断言）。
   **权限口径（本轮实测更正）**：`smoke.mjs` 要起 Electron，**必须完整权限**（受限沙箱下 mojo 命名管道被拦，
   报"壳状态文件超时"）；而**普通 node 脚本的输出在当前沙箱下可以正常管道与捕获**
   （`2>&1`、`$out = & node …` 均实测可用）—— 早先"node 输出一律不能重定向"的说法**过宽**，
   真正受限的是 **Electron 进程、以及以管道 stdio 派生子进程的程序**（`electron-builder` 的
   `@electron/rebuild` 报 `spawn EPERM` 即此，见坑 52）。
4. 任何改动：先跑相关检查点确认基线绿 → 改 → 重跑检查点 → 按 `docs/通用/03-AI协作与文档义务.md` §4 的文档同步矩阵更完所有文书 → commit。
5. 涉及**打包/asar 热更新/插件副本替换**：先征得用户同意（`docs/项目/02-架构与铁律.md` 铁律 1.3），绝不自行执行。
6. 会话内跑 electron 前：`Remove-Item Env:ELECTRON_RUN_AS_NODE`（`docs/项目/03-坑清单.md` 坑 2）。
7. 版本号：动手前确认 `VERSION` / `package.json` 与用户指定的一致；**未经用户允许不得推进版本号**（第 1.7 条铁律）。
